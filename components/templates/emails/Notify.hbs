<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .logo {
            max-width: 150px;
            height: auto;
        }
        .post-title {
            font-size: 24px;
            color: #2c3e50;
            margin-bottom: 15px;
        }
        .post-excerpt {
            color: #666;
            margin-bottom: 25px;
            font-size: 16px;
        }
        .cta-button {
            display: inline-block;
            padding: 12px 24px;
            background-color: #3498db;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin: 20px 0;
        }
        .custom-message {
            font-style: italic;
            color: #666;
            margin-bottom: 20px;
            padding: 10px;
            background-color: #f8f9fa;
            border-left: 4px solid #3498db;
        }
        .footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #eee;
            font-size: 12px;
            color: #666;
            text-align: center;
        }
        .tags {
            margin: 15px 0;
        }
        .tag {
            display: inline-block;
            background-color: #e9ecef;
            padding: 4px 8px;
            border-radius: 3px;
            margin-right: 5px;
            font-size: 12px;
            color: #495057;
        }
        @media (max-width: 600px) {
            body {
                padding: 10px;
            }
            .post-title {
                font-size: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        {{#if logo}}
            <img src="{{logo}}" alt="{{appName}}" class="logo">
        {{else}}
            <h1>{{appName}}</h1>
        {{/if}}
    </div>

    {{#if customMessage}}
        <div class="custom-message">
            {{customMessage}}
        </div>
    {{/if}}

    <div class="post-title">
        {{post.title}}
    </div>

    <div class="post-excerpt">
        {{post.excerpt}}
    </div>

    {{#if post.tags.length}}
        <div class="tags">
            {{#each post.tags}}
                <span class="tag">{{this}}</span>
            {{/each}}
        </div>
    {{/if}}

    <div style="text-align: center;">
        <a href="{{postUrl}}" class="cta-button">Read Full Post</a>
    </div>

    <div class="footer">
        <p>You're receiving this email because you're subscribed to {{appName}} notifications.</p>
        <p>
            <a href="{{unsubscribeUrl}}">Unsubscribe</a> |
            <a href="{{preferencesUrl}}">Manage Preferences</a>
        </p>
        <p>© {{currentYear}} {{appName}}. All rights reserved.</p>
    </div>
</body>
</html>
